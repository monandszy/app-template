spring:
  application:
    name: app-template-test
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      format_sql: true
logging:
  level:
    org:
#      springframework:
#        modulith: DEBUG
      hibernate:
        orm:
          jdbc:
            bind: TRACE
        SQL: DEBUG