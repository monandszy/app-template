spring:
  application:
    name: app-template-test
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      format_sql: true
  datasource:
    hikari:
      schema: ""
  liquibase:
    default-schema: ""
logging:
  level:
    org:
      springframework:
        modulith: DEBUG
      hibernate:
        orm:
          jdbc:
            bind: TRACE
        SQL: DEBUG